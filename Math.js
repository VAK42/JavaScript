'use strict';

let a = 4, b = 2;
let c = a + b;   // Addition
let d = a - b;   // Subtraction
let e = a * b;   // Multiplication
let f = a / b;   // Division
let g = a % b;   // Remainder
let h = a ** b;  // Exponentiation
