'use strict';

let a = 4, b = 2;
let c = a & b;    // AND
let d = a | b;    // OR
let e = a ^ b;    // XOR
let f = ~a;       // NOT
let g = a << 1;   // LEFT SHIFT
let h = a >> 1;   // RIGHT SHIFT
let i = a >>> 1;  // ZERO-FILL RIGHT SHIFT
